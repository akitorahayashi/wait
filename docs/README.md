# Documentation

This directory is the central documentation index for `wait`.

## Usage

- [Usage](usage.md): workflow examples, duration precedence, skip behavior, and cancellation behavior

The action usage describes duration resolution, skip behavior, and cancellation-aware waiting.

## Architecture

- [Architecture](architecture.md): runtime boundaries, dependency direction, execution flow, and failure invariants

## Configuration

- [Configuration](configuration.md): action inputs, defaults, output values, and validation semantics
